<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <?php
      /*The first data type that we have is string which can be used to store
        any string or phrase or text information.
      */
      $phrase = "To be or not to be";
      /*We can also store numerical information inside a variable which could be
        a whole number or a decimal number.
        Integer and floating-point numbers have a huge difference between them.
      */
      $age = 19; // integer
      $num = 19.0; // floating point number
      /*Another important data type that we have is Boolean.*/
      $isMale = true;
      /*Another thing which we have is more like a value which we are going to
        see in PHP and it is 'null'.
        It basically stands for no value.
      */
      // We can print numbers directly on the screen using the echo function.
      echo 3.344;
      // The rest can be printed as usual.
      echo "<br>";
      echo $phrase;
      echo "<br>";
      echo $num;
      echo "<br>";
      echo $isMale;
     ?>
  </body>
</html>
